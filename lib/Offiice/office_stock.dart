import 'package:flutter/material.dart';

class OfficeStockPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stock'),
      ),
      body: Center(
        child: Text('Stock Page'),
      ),
    );
  }
}
